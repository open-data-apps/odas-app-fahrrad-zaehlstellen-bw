{
  "$schema": "https://frictionlessdata.io/schemas/table-schema.json",
  "name": "odas-app-fahrrad-zaehlstellen-bw",
  "title": "Fahrrad-Zählstellen",
  "fields": [
    {
      "name": "iso_timestamp",
      "type": "datetime",
      "description": "Wird in der App verwendet",
      "constraints": {
        "required": true
      },
      "title": "Messzeitpunkt"
    },
    {
      "name": "counter_site",
      "type": "string",
      "description": "Wird in der App verwendet",
      "constraints": {
        "required": true
      },
      "title": "Zählstelle"
    },
    {
      "name": "domain_name",
      "type": "string",
      "description": "Wird in der App verwendet",
      "constraints": {
        "required": true
      },
      "title": "Gebiet"
    },
    {
      "name": "channels_all",
      "type": "integer",
      "description": "Wird in der App verwendet",
      "constraints": {
        "required": true
      },
      "title": "Zählungen gesamt"
    },
    {
      "name": "channels_in",
      "type": "integer",
      "description": "Wird in der App verwendet",
      "constraints": {
        "required": true
      },
      "title": "Zählungen eingehend"
    },
    {
      "name": "channels_out",
      "type": "integer",
      "description": "Wird in der App verwendet",
      "constraints": {
        "required": true
      },
      "title": "Zählungen ausgehend"
    },
    {
      "name": "latitude",
      "type": "number",
      "description": "Wird in der App verwendet",
      "constraints": {
        "required": true
      },
      "title": "Breitengrad"
    },
    {
      "name": "longitude",
      "type": "number",
      "description": "Wird in der App verwendet",
      "constraints": {
        "required": true
      },
      "title": "Längengrad"
    }
  ]
}
